# Optional build-time overrides for Astro.
# Runtime newsletter secrets belong in `.dev.vars`, not `.env`.

# Override the canonical site URL used for metadata and sitemap generation.
SITE_URL=https://alpsconference.com

# Optional subfolder deploy path, e.g. /conference/
# BASE_PATH=/conference/
